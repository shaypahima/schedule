## Agent skills

### Issue tracker

Issues live in the `shaypahima/velofit` GitHub repo, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles, mapping `ready-for-agent`→`afk-ready` and `ready-for-human`→`hitl` to reuse existing repo labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
